/**
 * Twitter card = the OG card. The root layout config points twitter.images at
 * the root /opengraph-image URL; this file convention keeps the X preview
 * identical to the OG preview and locally owned by the same component.
 */
export { default, alt, size, contentType } from "./opengraph-image";
